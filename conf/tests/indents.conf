# debug-line-extractor.conf

# custom
# ;
# ; # error
what
what ;what;       what;server{#} }
	index what.html#
;}

server { listen 8001; root /var/www/html;
	location /test/

	{ index index.html; } location /images/ {}
} index none.html;server{listen 8002;} 	server


# ; # error
# ; # error

{

listen 8003; index index.nginx-debian.html autoindex on;}
index
autoindex on # its not an error, but "on" should not be params here.
index nginx-debian.html #
;server{}server { listen 8004; server_name test2;} # error
